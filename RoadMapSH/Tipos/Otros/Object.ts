// - Los tipos objetos se refieren a cualquier 'cosa' o 'valor' con propiedades, es decir, !casi todos!.

function imprimeCoordenadas(pt: { x: number; y: number }) {
  console.log("Las coordenadas de x son: " + pt.x);
  console.log("Las coordenadas de y son: " + pt.y);
}

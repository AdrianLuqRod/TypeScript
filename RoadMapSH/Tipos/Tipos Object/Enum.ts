// - Enum en TypeScript permite definir un conjunto de constantes con nombre.
// - Los valores de los miembros de un enum pueden ser cadenas o números.

enum Direccion {
  Up = 1,
  Down,
  Left,
  Right,
}
